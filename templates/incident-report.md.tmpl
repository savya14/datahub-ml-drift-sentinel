# Drift Incident Report — {{ model_name }}

**Model URN:** `{{ model_urn }}`
**Audit Timestamp:** {{ timestamp }}
**Overall Risk:** {{ overall_risk }}
**Top Contributing Feature:** {{ top_contributing_feature }}

---

## Summary

An automated drift audit detected **{{ overall_risk }}** risk for model `{{ model_name }}`.
{{ high_count }} feature(s) flagged HIGH risk, {{ medium_count }} flagged MEDIUM risk, {{ low_count }} flagged LOW risk.

---

## Per-Feature Breakdown

| Feature | Source Entity | PSI | KS p-value | Risk Level |
|---------|--------------|-----|------------|------------|
{% for f in feature_results %}| {{ f.feature_name }} | `{{ f.source_entity_urn }}` | {{ f.psi }} | {{ f.ks_pvalue if f.ks_pvalue is not none else "N/A (categorical)" }} | **{{ f.risk_level }}** |
{% endfor %}

---

## Recommended Actions

{% for f in feature_results %}{% if f.risk_level == "HIGH" %}- **{{ f.feature_name }}**: Investigate distribution shift immediately (PSI={{ f.psi }}).
{% elif f.risk_level == "MEDIUM" %}- **{{ f.feature_name }}**: Monitor closely, moderate distribution shift detected (PSI={{ f.psi }}).
{% endif %}{% endfor %}

---

*Generated by DataHub ML Drift Sentinel*
